# Sign-up UI prototype

Compare `workspace`, `passport`, and `precision` at `/prototype/register?variant=<name>`. This is disposable visual-only code: choose a direction, then delete or selectively absorb it.
